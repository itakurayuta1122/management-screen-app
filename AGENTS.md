# 管理画面アプリ — エージェント定義とプロジェクト指針

本リポジトリにおける開発エージェントの役割とプロジェクト固有のルールを定義します。

---

## 1. エージェントの役割と行動指針

管理画面アプリ（Admin Dashboard）の開発をサポートするエンジニアとして、以下の指針で行動してください。

### 1-1. チャット内容の判定

ユーザーからメッセージを受け取ったら、最初にそのチャットが以下のどちらに該当するかを判定してください。

- **実装タスク**
  - コードの作成・変更・削除を依頼された場合
  - 機能追加・修正・リファクタリングを依頼された場合
  - ファイル作成・変更など、実際にプロジェクトへ変更を加える場合

- **実装タスク以外**
  - 実装に関する質問
  - コードや設計についての疑問・相談
  - 技術的な調査・確認
  - 「これはどういう意味？」などの学習目的の質問
  - 実装を伴わない説明・相談

判定後、それぞれに定められた手順を実行してください。

### 1-2. 共通ルール

実装タスク・実装タスク以外のどちらの場合でも、回答や作業を行う前に、必要に応じてプロジェクトの設計・実装状況を確認し、既存の仕様や実装と矛盾しないようにしてください。

### 1-3. 実装タスクの場合

- **設計ベースの実装**: 実装を開始する前に、必ず `docs/SPECS.md` のデータ設計および機能一覧を確認してください。
- **タスク管理**: 実装順序は `docs/TODO.md` に従ってください。各コンポーネントや機能の実装は設計書のコンポーネント設計に基づいてください。
- **技術スタックの遵守**: React + TypeScript + Next.js (App Router) のベストプラクティスを遵守してください。特にステート管理や最適化（`useMemo`など）については設計書の要件を満たす実装を行ってください。
- **実装後の検証**: 実装後は必ずコンパイル・テストを実行してください。エラーが発生した場合は原因を調査して修正し、再度コンパイル・テストを実行してください。
- **コンテキストの保持**: 学習した内容を応用してコードを記述し、リファクタリングを積極的に行ってください。

### 1-4. 実装タスク以外の場合

実装に関する疑問や質問が発生したチャットのやり取りは、**必ず都度、例外なく**、下記の手順で対応してください。

1. 質問内容と回答内容の要点を整理する。
2. `README.md` の「質問と回答（Q&A）」を確認する。
3. 既存のQ&Aと重複していないことを確認する。
4. 重複していない場合は、質問者と回答者（AI）の要点をQ&A形式で `README.md` に追加する。
5. 重複している場合は、新しい項目を追加せず、既存のQ&Aを維持する。

※ AIによる実装のやり取りそのものを記録する必要はありません。
質問と回答の要点のみをQ&A形式で記録してください。

**例:**
### Q: `export default function Home()` は自動で実行される関数ですか？
A: はい。Next.js (App Router) では、`app/page.tsx` で `export default` された関数は、そのパス（ルートURL `/`）へユーザーがアクセスした際、Next.jsによって自動的に呼び出され、UIとして描画（レンダリング）されます。

---

## 2. GitHub Pages デプロイに関する注意事項

このプロジェクトは `https://itakurayuta1122.github.io/management-screen-app/` にデプロイされています。GitHub Pages はサブパス（`/management-screen-app/`）でホスティングされるため、以下の設定が必須です。

### 1. `next.config.ts` の `basePath` / `assetPrefix` 設定

```ts
const repo = "management-screen-app";

const nextConfig: NextConfig = {
  output: "export",
  basePath: `/${repo}`,
  assetPrefix: `/${repo}/`,
  images: { unoptimized: true },
};
```

### 2. `next/image` の画像パスには `basePath` を手動で付ける

`next/image` コンポーネントは **`basePath` を自動適用しない** ため、環境変数 `NEXT_PUBLIC_BASE_PATH` を活用してください。

**`.env.production`**
```
NEXT_PUBLIC_BASE_PATH=/management-screen-app
```

**コード例**
```tsx
const basePath = process.env.NEXT_PUBLIC_BASE_PATH ?? "";

<Image src={`${basePath}/example.png`} ... />
```

### 3. GitHub Pages の Jekyll 問題（最重要）

`gh-pages` の `--nojekyll` オプションを使用して、`.nojekyll` ファイルを自動生成し、Jekyll を無効化してください。

```json
// package.json
"deploy": "gh-pages -d out --nojekyll -f"
```

